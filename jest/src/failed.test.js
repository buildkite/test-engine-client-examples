describe('failed test', () => {
  test('adds 1 + 2 to equal 2', () => {
    expect(1 + 2).toBe(2);
  });
});

describe('passing test', () => {
  test('adds 3 + 4 to equal 7', () => {
    expect(3 + 4).toBe(7);
  });
});
