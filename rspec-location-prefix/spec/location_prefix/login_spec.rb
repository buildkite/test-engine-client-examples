require "spec_helper"

RSpec.describe "Login" do
  it "logs in a user" do
    sleep(1)
    expect(true).to be true
  end

  it "redirects to the dashboard" do
    sleep(1)
    expect(true).to be true
  end
end
